// The section header only. Sub-navigation lives in the sidebar now, so there is
// no second row of tabs competing with it
export default function DevelopersLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
